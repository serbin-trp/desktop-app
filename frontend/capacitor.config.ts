import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.serbin.finap',
  appName: 'frontend',
  webDir: 'dist/frontend/browser'
};

export default config;
